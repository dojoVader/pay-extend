# Turbo Monorepo – Vue + NestJS Starter

This is a full-stack monorepo powered by [Turborepo](https://turbo.build/), featuring:

- ⚡️ **Vue 3** (Vite) frontend
- 🚀 **NestJS** backend
- 📦 Shared packages and utilities
- 🔁 Fast builds, caching, and development using Turborepo

## 🚀 Getting Started

### 1. Install Dependencies

Using your preferred package manager (I am using `pnpm`):

```bash
pnpm install

### 2. Start Development Servers

Start the frontend and backend development servers:

```bash
pnpm dev
```

### 3. Build and Test

Build the frontend and backend:

```bash
pnpm build
```

Run tests:

```bash
pnpm test
```
